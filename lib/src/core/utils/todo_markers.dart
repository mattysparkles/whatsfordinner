abstract final class TodoMarkers {
  static const apiIntegration = 'TODO(api): Replace mock implementation with production API adapter.';
  static const persistenceIntegration =
      'TODO(persistence): Replace in-memory stores with local database-backed repositories.';
}
